[bridge-version-policy]
protocol = sbt-bridge
version = v1
rule = if any file under bridge-fixtures/sbt-bridge changes, build.sbt version must also change
breaking-change-rule = breaking changes require a new protocol version directory such as v2
source-of-truth = /Users/asami/src/dev2025/cozy/bridge/sbt-bridge
